{
	"version": 2,
	"buildCommand": "tsc && cp package.json dist/",
	"builds": [
		{
			"src": "src/index.ts",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "/dist/index.js",
			"methods": ["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"]
		}
	]
}

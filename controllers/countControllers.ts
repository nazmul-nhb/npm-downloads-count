import { Request, Response, NextFunction } from "express";

const getDownloadsCount = async (
	req: Request,
	res: Response,
	next: NextFunction
) => {
    try {
        
    } catch (error) {
        
    }
};
